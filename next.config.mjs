/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com',"plus.unsplash.com"], // Add Unsplash's domain here
    },
  };
  
export default nextConfig
  